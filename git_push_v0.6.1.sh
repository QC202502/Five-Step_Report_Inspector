#!/bin/bash

# 设置颜色
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
NC='\033[0m' # No Color

echo -e "${YELLOW}准备提交五步法研报分析器 v0.6.1 更新...${NC}"

# 添加所有更改的文件
git add .

# 提交更改
git commit -m "feat: 实现研报推荐功能 v0.6.1

- 添加研报推荐功能，基于五步法评分、时间新鲜度和行业相关性进行智能推荐
- 新增推荐设置页面，允许用户调整推荐因素权重和偏好行业
- 添加已读/未读状态管理，支持标记研报为已读或不感兴趣
- 实现推荐弹窗，登录时显示最高推荐分数的研报"

# 推送到远程仓库
echo -e "${GREEN}正在推送更改到远程仓库...${NC}"
git push origin main

echo -e "${GREEN}完成! v0.6.1 已成功提交并推送。${NC}" 